extends ./_layout.jade

block content
  .wrapper
    .layout-side
      +menu()
    .layout-content._flex._wrap
      +info-message
      .layout-content__left
        +balance-block()
      .layout-content__right
        +wallet-block()
      +last-paid()
        